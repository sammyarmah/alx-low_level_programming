#include "main.h"

/**
 * _isupper - checks whether a letteris upper
 * @c: number to be checked
 * Return: 1 for upper and 0 for any number
 */

int _isupper(int c)
{

	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
