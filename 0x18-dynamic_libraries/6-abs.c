#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer.
 *
 *@a: positive or negative integer supplied.
 *
 * Return: Always 0
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-a);
	}
}

